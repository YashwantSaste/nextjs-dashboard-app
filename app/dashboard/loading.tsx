import DashboardSkeleton from "../ui/skeletons"
export default function Loading(){
    return(
        // <div>Loading...</div> -->this was the first loading screen code and now we will add the skeleton loading
        <DashboardSkeleton/>
    )
}